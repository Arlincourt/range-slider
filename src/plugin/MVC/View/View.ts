import IState from '../../types/IState';
import Observer from '../../Observer/Observer';

class View {
  observer?: Observer;

  constructor() {

  }

  public update(state: IState) {

  }
}

export default View;
